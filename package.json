{
  "name": "queuesmart-pro",
  "version": "1.0.0",
  "private": true,
  "description": "QueueSmart Pro full-stack queue prediction and management system",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "concurrently \"npm run dev --workspace backend\" \"npm run start --workspace frontend\"",
    "server": "npm run dev --workspace backend",
    "client": "npm run start --workspace frontend",
    "build": "npm run build --workspace frontend"
  },
  "keywords": [
    "queue",
    "react",
    "express",
    "socket.io",
    "mongodb"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
